
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://myrefresh.org',
  generateRobotsTxt: true, // It’ll generate robots.txt too
  changefreq: 'weekly',
  priority: 0.7,
};

export default config
